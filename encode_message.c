#include "Talkthrough.h"

/*void encodeMessage(void) {

	for(int i = 0; i < 2; i++) {
		for(int k = 7; k >= 0; k--) {
			int bit = ((proc_string[i]) & (1 << k)) ? 1 : 0; // Extract each bit from the character
			proc_enc[i * 7 + 7 - k] = bit;
		}
	}
}*/
